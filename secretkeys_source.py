"""
secretkeys.py - secret key for CREM.

Replace the string below with a secret key. For security reasons,
do *not* place secretkeys.py under version control.

To generate a secret key, issue the following commands in the Python
interpreter:

    import os
    os.urandom(24)

"""

SECRET_KEY = 'Replace_this_string_with_a_secret_key'
